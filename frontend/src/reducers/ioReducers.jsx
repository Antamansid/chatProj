import * as ioConstants from "../constants/ioConstants.jsx";

export function ioReducers(state = {}, action){
  switch (action.type){
    case ioConstants.CONNECT_TO_CHAT_FULFILLED:{
      break;
    }
  }
  return state;
}